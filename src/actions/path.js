export const dashboard=(newPath)=>({
  type:'DASHBOARD',
  newPath
})

export const home=(newPath)=>({
  type:'HOME',
  newPath
})
export const create=(newPath)=>({
  type:'CREATE',
  newPath
})
export const edit=(newPath)=>({
  type:'EDIT',
  newPath
})